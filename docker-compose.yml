version: '3'
services:
  postgres:
    container_name: postgres
    image: postgres
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
    ports:
      - "5432:5432"
  app:
    container_name: server
    build: .
    ports:
      - "3000:3000"
    links:
      - postgres